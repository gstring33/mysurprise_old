{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <div class="row">
        {% for flashError in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flashError }}</div>
        {% endfor %}
        <div class="col-md-12">
            <h1>Hallo {{ app.user.firstname }} ! </h1>
            <h2>Willkommen bei "Das perfekte Geschenk"</h2>
            <p>Bald kannst du deinen Wunschzettel erstellen und die Person ziehen, die dein Geschenk erhalten wird. Aber zuerst musst du dein Passwort zurücksetzen. Gib deine E-Mail-Adresse ein, um den Link zum Ändern deines Passworts zu erhalten.</p>
        </div>
        <div class="col-md-12 mt-5">
            <h3 class="title-secondary">Passwort zurücksetzen</h3>
            <form name="reset_password_request_form" method="POST">
                {{ form_start(requestForm) }}
                        <div class="form-group">
                            <label for="reset_password_request_form_username" class="required">Benutzername</label>
                            {{ form_row(requestForm.username) }}
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">Send password reset email</button>
                        </div>
                {{ form_end(requestForm) }}
            </form>
        </div>
    </div>
{% endblock %}