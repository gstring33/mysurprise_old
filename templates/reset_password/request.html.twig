{% extends 'base.html.twig' %}

{% block title %}Renouvellement du mot de passe{% endblock %}

{% block body %}
    <div class="row">
        {% for flashError in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flashError }}</div>
        {% endfor %}
        <div class="col-md-12">
            <h1>Salut {{ app.user.firstname }} ! </h1>
            <h2>Bienvenue dans my-surprise.com</h2>
            <p>Tu vas pouvoir bientot créer ta liste et tirer au sort la personne à qui tu offriras ton cadeau. Mais tout d'abord tu dois choisir un nouveau mot de passe. Saisi ton nom d'utilisateur pour recevoir l'email avec le lien permettant de changer ton mot de passe.</p>
        </div>
        <div class="col-md-12 mt-5">
            <h3 class="title-secondary">Changer mon mot de passe</h3>
            <form name="reset_password_request_form" method="POST">
                {{ form_start(requestForm) }}
                        <div class="form-group">
                            <label for="reset_password_request_form_username" class="required">Nom d'utilisateur</label>
                            {{ form_row(requestForm.username) }}
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">Envoyer email</button>
                        </div>
                {{ form_end(requestForm) }}
            </form>
        </div>
    </div>
{% endblock %}